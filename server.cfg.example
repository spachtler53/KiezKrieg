# Sample server.cfg configuration for KiezKrieg Framework
# Add these lines to your existing server.cfg

# KiezKrieg Framework Configuration
# Ensure these resources are loaded in the correct order

# Core Dependencies (must be loaded first)
ensure oxmysql
ensure es_extended

# KiezKrieg Resources (load in this order)
ensure kk-core
ensure kk-ui  
ensure kk-zones
ensure kk-admin
ensure kk-factions

# Optional: Add permissions for admin groups
add_ace group.admin kk.admin.all allow
add_ace group.superadmin kk.admin.all allow
add_ace group.owner kk.admin.all allow

# Database connection (adjust to your settings)
set mysql_connection_string "mysql://username:password@localhost/database_name?charset=utf8mb4"

# Server settings
set gamename "KiezKrieg Gangwar Server"
sets tags "kiezkrieg,gangwar,ffa,factions,esx"

# Game settings
set onesync on
set onesync_enabled 1
set onesync_population 0

# Performance settings
set sv_maxclients 64
set sv_enforceGameBuild 2802